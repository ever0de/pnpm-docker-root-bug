{
  "private": true,
  "scripts": {
    "postinstall": "pnpm onflow build",
    "consumer": "pnpm --filter \"consumer\"",
    "onflow": "pnpm --filter \"onflow\"",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "clean": "rm -rf **/node_modules **/dist **/lib"
  },
  "dependencies": {},
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "^3.2.0",
    "@types/node": "^18.0.1",
    "prettier": "^2.6.2",
    "ts-node": "^10.8.1",
    "typescript": "^4.7.3"
  }
}
